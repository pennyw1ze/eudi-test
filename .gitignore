# vendored official EU reference implementations (cloned by `make vendor`)
/vendor/

# build output
build/
.gradle/
node_modules/
dist/
*.class
*.jar
!**/gradle/wrapper/gradle-wrapper.jar

# runtime state (per-run crypto + network traces, participant list)
/runs/
runs/
*.log
.env.local

# editors
.idea/
.vscode/
*.swp
gateway/certs/
config/status-list/app.zip
diag.txt
issuer.log
keycloak.log

# IDE/compiled output
wallet-core/bin/

# unpublished paper — never commit
docs/no_pooling_no_party.tex
*.tex
